fn main() {
    // declaration using const keyword
    // type of value must be annotated
    // constants may set only constant expression
    const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;

    println!("How many seconds in 3 hour? {THREE_HOURS_IN_SECONDS}");
}
